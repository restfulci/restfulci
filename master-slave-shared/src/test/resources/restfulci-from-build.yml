version: 1.0

environment:
  build:
    context: ./path/to/subproject
    dockerfile: ./Dockerfile

command:
  - bash
  - -c
  - "\"echo 0\""

results:
  - type: junit
    path: target/surefile-reports
  - type: junit
    path: target/failsafe-reports
