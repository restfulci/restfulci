apiVersion: v1

kind: Service
metadata:
  name: restfulci-master-api-server
  labels:
    app: master-api-server
spec:
  type: LoadBalancer
  selector:
    app: master-api-server
  ports:
  - port: 80
    targetPort: 8080
